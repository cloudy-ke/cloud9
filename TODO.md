# TODO - Product Carousel Implementation

- [x] Add carousel HTML section in `index.html` above the products grid.
- [x] Add carousel styling in `css/style.css` (track, slides, arrows, dots, responsive behavior).
- [x] Add carousel JavaScript logic in `js/main.js` using all non-logo images from `images/`.
- [x] Verify functionality and mark all tasks complete.
